name: Terraform Infrastructure

on:
  push:
    branches: [ main, master ]
    paths:
      - 'infrastructure/terraform/**'
      - '.github/workflows/terraform.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'infrastructure/terraform/**'
      - '.github/workflows/terraform.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - preprod
          - production
        default: preprod
      action:
        description: 'Terraform action'
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy
        default: plan

# Cancel in-progress terraform runs
concurrency:
  group: terraform-${{ github.event.inputs.environment || 'auto' }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TF_VERSION: '1.9'
  TF_WORKING_DIR: 'infrastructure/terraform/environments'
  # Environment configuration - values come from tfvars files
  ENVIRONMENTS: |
    {
      "preprod": {
        "project_id": "astute-strategy-406601",
        "tf_state_bucket": "astute-strategy-406601-tf-state",
        "tf_state_prefix": "preprod/state"
      },
      "production": {
        "project_id": "perundhu-prod-001",
        "tf_state_bucket": "perundhu-prod-001-tf-state-1767644488",
        "tf_state_prefix": "production/state"
      }
    }

jobs:
  terraform-validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}
    
    - name: Terraform Format Check
      working-directory: ${{ env.TF_WORKING_DIR }}
      run: terraform fmt -check -recursive
      continue-on-error: true
    
    - name: Validate PreProd Config
      working-directory: ${{ env.TF_WORKING_DIR }}/preprod
      run: |
        terraform init -backend=false -lock=false
        terraform validate
    
    - name: Validate Production Config
      working-directory: ${{ env.TF_WORKING_DIR }}/production
      run: |
        terraform init -backend=false -lock=false
        terraform validate

  terraform-plan:
    name: Plan Terraform - ${{ matrix.environment }}
    runs-on: ubuntu-latest
    needs: [terraform-validate]
    if: |
      (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')) ||
      (github.event_name == 'pull_request') ||
      (github.event_name == 'workflow_dispatch')
    
    strategy:
      matrix:
        include:
          - environment: preprod
            tf_dir: infrastructure/terraform/environments/preprod
            project_id: astute-strategy-406601
      max-parallel: 1
    
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    
    env:
      TF_ENV_DIR: ${{ matrix.tf_dir }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET_PREPROD }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}
    
    - name: Debug Environment Info
      run: |
        echo "Environment: ${{ matrix.environment }}"
        echo "Working Directory: ${{ env.TF_ENV_DIR }}"
        echo "Project ID: ${{ matrix.project_id }}"
        echo "GitHub Event: ${{ github.event_name }}"
    
    - name: Terraform Init
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform init -input=false -reconfigure -lock=false
    
    - name: Terraform Plan
      id: plan
      working-directory: ${{ env.TF_ENV_DIR }}
      run: |
        # Use terraform.tfvars file from environment directory
        # No need to pass -var flags - tfvars file has all variables
        terraform plan -no-color -out=tfplan -lock=false
        terraform show -no-color tfplan > tfplan.txt
    
    - name: Upload Plan
      uses: actions/upload-artifact@v4
      with:
        name: terraform-plan-${{ matrix.environment }}
        path: ${{ env.TF_ENV_DIR }}/tfplan
        retention-days: 1
    
    - name: Comment Plan on PR
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const fs = require('fs');
          const path = require('path');
          const planFile = path.join('${{ env.TF_ENV_DIR }}', 'tfplan.txt');
          
          let planContent = 'Plan file not found';
          try {
            planContent = fs.readFileSync(planFile, 'utf8');
          } catch (error) {
            console.log('Error reading plan file:', error.message);
          }
          
          const truncatedPlan = planContent.length > 65000 ? planContent.substring(0, 65000) + '\n\n... (truncated)' : planContent;
          
          const output = `#### Terraform Plan 📋 \`${{ matrix.environment }}\`
          <details><summary>Show Plan</summary>

          \`\`\`terraform
          ${truncatedPlan}
          \`\`\`

          </details>

          *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: output
          });

  terraform-plan-production:
    name: Plan Terraform - production
    runs-on: ubuntu-latest
    needs: [terraform-validate]
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production'
    
    env:
      TF_ENV_DIR: infrastructure/terraform/environments/production
    
    permissions:
      contents: read
      id-token: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}
    
    - name: Debug Environment Info
      run: |
        echo "Environment: production"
        echo "Working Directory: ${{ env.TF_ENV_DIR }}"
        echo "Project ID: perundhu-prod-001"
        echo "GitHub Event: ${{ github.event_name }}"
    
    - name: Terraform Init
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform init -input=false -reconfigure -lock=false
    
    - name: Terraform Plan
      id: plan
      working-directory: ${{ env.TF_ENV_DIR }}
      run: |
        terraform plan -no-color -out=tfplan -lock=false
        terraform show -no-color tfplan > tfplan.txt
    
    - name: Upload Plan
      uses: actions/upload-artifact@v4
      with:
        name: terraform-plan-production
        path: ${{ env.TF_ENV_DIR }}/tfplan
        retention-days: 1


  terraform-apply:
    name: Apply Terraform - ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    needs: [terraform-plan, terraform-plan-production]
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' && 
      github.event.inputs.action == 'apply' &&
      (
        (github.event.inputs.environment == 'preprod' && needs.terraform-plan.result == 'success') ||
        (github.event.inputs.environment == 'production' && needs.terraform-plan-production.result == 'success')
      )
    
    # Only run the selected environment
    environment:
      name: ${{ github.event.inputs.environment }}-infrastructure
      url: ${{ github.event.inputs.environment == 'preprod' && 'https://console.cloud.google.com/run?project=astute-strategy-406601' || 'https://console.cloud.google.com/run?project=perundhu-prod-001' }}
    
    permissions:
      contents: read
      id-token: write
    
    env:
      TF_ENV_DIR: infrastructure/terraform/environments/${{ github.event.inputs.environment }}
      PROJECT_ID: ${{ github.event.inputs.environment == 'preprod' && 'astute-strategy-406601' || 'perundhu-prod-001' }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Authenticate to Google Cloud (preprod)
      if: github.event.inputs.environment == 'preprod'
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET_PREPROD }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Authenticate to Google Cloud (production)
      if: github.event.inputs.environment == 'production'
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}
    
    - name: Terraform Init
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform init -input=false
    
    - name: Download Plan
      uses: actions/download-artifact@v4
      with:
        name: terraform-plan-${{ github.event.inputs.environment }}
        path: ${{ env.TF_ENV_DIR }}
    
    - name: Verify Plan File
      working-directory: ${{ env.TF_ENV_DIR }}
      run: |
        if [ ! -f tfplan ]; then
          echo "ERROR: tfplan file not found in ${{ env.TF_ENV_DIR }}"
          exit 1
        fi
        echo "✓ Plan file verified"

    - name: Ensure Cloud SQL instance is imported
      working-directory: ${{ env.TF_ENV_DIR }}
      env:
        INSTANCE_NAME: ${{ github.event.inputs.environment == 'preprod' && 'perundhu-preprod-mysql-us' || 'perundhu-production-mysql-us' }}
      run: |
        echo "Checking Terraform state for Cloud SQL instance..."
        if ! terraform state list | grep -q "module.database.google_sql_database_instance.mysql_instance"; then
          echo "Instance not in state; importing ${INSTANCE_NAME}..."
          terraform import 'module.database.google_sql_database_instance.mysql_instance' ${PROJECT_ID}/${INSTANCE_NAME}
          echo "Import completed. Re-planning to align state..."
          terraform plan -no-color -out=tfplan -lock=false
        else
          echo "Instance already in state. Re-planning to ensure no create actions..."
          terraform plan -no-color -out=tfplan -lock=false
        fi

    - name: Ensure SQL instance is running
      env:
        INSTANCE_NAME: ${{ github.event.inputs.environment == 'preprod' && 'perundhu-preprod-mysql-us' || 'perundhu-production-mysql-us' }}
      run: |
        CURRENT=$(gcloud sql instances describe ${INSTANCE_NAME} --project ${PROJECT_ID} --format='value(settings.activationPolicy)' || echo "")
        echo "Current activationPolicy=${CURRENT}"
        if [ "$CURRENT" = "NEVER" ] || [ -z "$CURRENT" ]; then
          echo "Patching activationPolicy to ALWAYS to allow updates..."
          gcloud sql instances patch ${INSTANCE_NAME} --project ${PROJECT_ID} --activation-policy=ALWAYS --quiet || true
        else
          echo "Instance already running."
        fi
        echo "Re-planning after potential start..."
        cd ${{ env.TF_ENV_DIR }} && terraform plan -no-color -out=tfplan -lock=false

    - name: Terraform Apply
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform apply -auto-approve tfplan
    
    - name: Terraform Output
      id: output
      working-directory: ${{ env.TF_ENV_DIR }}
      run: |
        terraform output -json > outputs.json
        echo "Outputs saved"
    
    - name: Upload Terraform Outputs
      uses: actions/upload-artifact@v4
      with:
        name: terraform-outputs-${{ github.event.inputs.environment }}
        path: ${{ env.TF_ENV_DIR }}/outputs.json
        retention-days: 1
    
    - name: Create Summary
      run: |
        echo "## ✅ ${{ github.event.inputs.environment }} Infrastructure Applied" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Environment**: ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Project**: ${{ env.PROJECT_ID }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Applied by**: ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "Check [Cloud Console](https://console.cloud.google.com/run?project=${{ env.PROJECT_ID }}) for resources" >> $GITHUB_STEP_SUMMARY

  terraform-destroy:
    name: Destroy Terraform - ${{ matrix.environment }}
    runs-on: ubuntu-latest
    needs: [terraform-plan, terraform-plan-production]
    if: |
      always() && (
        (github.event_name == 'workflow_dispatch' && 
        github.event.inputs.action == 'destroy' &&
        github.event.inputs.environment == 'preprod') ||
        (github.event_name == 'workflow_dispatch' && 
        github.event.inputs.action == 'destroy' &&
        github.event.inputs.environment == 'production')
      )
    
    strategy:
      matrix:
        include:
          - environment: preprod
            tf_dir: infrastructure/terraform/environments/preprod
            project_id: astute-strategy-406601
          - environment: production
            tf_dir: infrastructure/terraform/environments/production
            project_id: perundhu-prod-001
      max-parallel: 1
    
    environment:
      name: ${{ matrix.environment }}-infrastructure-destroy
    
    permissions:
      contents: read
      id-token: write
    
    env:
      TF_ENV_DIR: ${{ matrix.tf_dir }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Authenticate to Google Cloud (preprod)
      if: matrix.environment == 'preprod'
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET_PREPROD }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Authenticate to Google Cloud (production)
      if: matrix.environment == 'production'
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCPSECRET }}
        create_credentials_file: true
        export_environment_variables: true
    
    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}
    
    - name: Terraform Init
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform init -input=false
    
    - name: Terraform Destroy
      working-directory: ${{ env.TF_ENV_DIR }}
      run: terraform destroy -auto-approve -lock=false
    
    - name: Create Summary
      run: |
        echo "## ⚠️ ${{ matrix.environment }} Infrastructure Destroyed" >> $GITHUB_STEP_SUMMARY



