// Mock functions for tests
export const mockFunction = jest.fn();
export const anotherMockFunction = jest.fn();

// Add any other mock functions that tests might be expecting
export const searchBuses = jest.fn();
export const getBusDetails = jest.fn();
export const updateBusLocation = jest.fn();